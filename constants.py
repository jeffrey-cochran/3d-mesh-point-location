from os.path import join, dirname, abspath

data_dir = join(dirname(abspath(__file__)), "data")
mesh_dir = join(data_dir, "mesh")
query_dir = join(data_dir, "query")
test_data_dir = join(data_dir, "test_data")
